import React, { PureComponent } from 'react';

export default function LobbyHeader() {
  return (
    <div className="lobby-header">
      <span className="game-lobby caps">Game Lobby</span>
      <span className="select-game">Select your game</span>
    </div>
  );
}
